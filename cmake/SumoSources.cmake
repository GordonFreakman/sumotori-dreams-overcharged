set(SUMO_SOURCES
  src/core.cpp      # math, containers, assertions
  src/game.cpp      # runtime, fighters, levels, replay, scene
  src/parser.cpp    # the .smo compiler
  src/physics.cpp   # boxes, collision, water, fracture
  src/render.cpp    # camera, projection, procedural textures, screenshot
)
